<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.10.xsd">

  <changeSet author="honnel" id="mail-notification-absence-colleagues">

    <preConditions>
      <tableExists tableName="person_notifications"/>
      <tableExists tableName="person_permissions"/>
    </preConditions>

    <sql>
        INSERT INTO person_notifications(person_id, notifications) SELECT DISTINCT person_id, 'NOTIFICATION_EMAIL_COLLEAGUES' FROM person_permissions perm WHERE perm.permissions = 'USER' OR perm.permissions = 'INACTIVE';
    </sql>
  </changeSet>
</databaseChangeLog>
